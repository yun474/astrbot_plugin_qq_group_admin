# CHANGELOG

## 2.3.0 - 2026-08-12

- 新增 `silent_mute_success_notice` 配置，仅控制禁言、解禁指令是否直接发送固定成功通知。
- 禁言、解禁工具始终将结果交回 LLM 继续自然回复，工具自身不直接发送固定成功通知。
- 修复 LLM 工具返回消息结果导致 Agent Loop 提前结束的问题，所有工具改为返回文本供 LLM 继续自然回复。
- LLM 群管工具改为以机器人自身权限执行，机器人账号发出的群管指令也默认有权限。
- 兼容 AstrBot 4.26.0 向 LLM 工具传入 `ContextWrapper` 的调用方式。
- 调整禁言指令为零位置参数、自解析消息结构，兼容 LLM Executor 注入的 At 组件与纯文本 OpenID。
- 移除 AstrBot 版本号限制，改为按运行时对象能力兼容不同工具调用链。

## 2.2.0 - 2026-08-12

- 新增 `/解禁 @用户` 指令，可解除一个或多个被艾特成员的禁言。
- 新增 `qq_group_unmute_member` LLM 工具及独立配置开关。
- 新增 `/禁言状态` 和 `qq_group_get_mute_status`，展示全员禁言规则及当前被禁言成员。
- `/群管帮助` 改为分区 Markdown 格式，发送失败时自动降级为纯文本。
- 默认禁言时长由 `10分` 调整为 `1分`，仍可通过配置修改并实时显示在帮助中。

## 2.1.0 - 2026-08-12

- `/禁言 @用户` 允许省略时间，并读取可配置的默认禁言时长。
- 禁言 LLM 工具省略时长时同样使用默认值。
- 新增 `default_mute_duration` 配置项，默认 `10分`。
- 新增 `/群管帮助`，展示指令、当前默认禁言时长、时间格式和入群申请回复审批方式。

## 2.0.0 - 2026-08-12

- 仓库与插件更名为 `astrbot_plugin_qq_group_admin`。
- 新增 QQ 官方群成员禁言/解禁指令与 LLM 工具。
- 新增入群申请事件通知、群管回复审批、申请列表和审批 LLM 工具。
- 新增按群保存的插件群管，AstrBot 管理员默认拥有全局权限。
- 整合原有普通成员进退群通知，并继续支持 WebSocket/Webhook 事件桥。
- 进退群模板精简为只支持 `{member_at}`；退群事件没有昵称且无法真正艾特，届时仅显示成员 OpenID。
- 新增各功能独立配置开关和持久化申请映射。
- 适配 2026-08-10 启用的 `api.bot.qq.com` OpenAPI 域名。

## 1.0.3 - 2026-07-15

- 修复 `{member_at}`、`{operator_at}` 仍使用即将弃用的 `<@id>`，且按普通文本发送导致 QQ 客户端无法渲染真实 @ 的问题。
- 改用官方 `<qqbot-at-user id="..." />` 标签；包含 @ 占位符的通知自动通过 Markdown 消息发送。
- 成员进群欢迎的默认模板改为 `欢迎 {member_at} 加入 {group_display}！`。
- Markdown @ 发送失败时自动降级为昵称/OpenID 普通文本，避免群里出现裸标签。
- 增加真实 @ 发送和失败降级回归测试。

## 1.0.2 - 2026-07-15

- 修复 WebSocket 模式只安装事件解析器、却没有订阅 `GROUP_MEMBER` Intents（`1 << 24`），导致普通成员进退群事件根本不会下发的问题。
- 启动时保留原有 Intents 并补充成员事件订阅位；若连接已建立，会明确提示重载 QQ 平台或重启 AstrBot。
- README 补充 WebSocket 与 Webhook 的选择、HTTP 回调配置和事件权限说明。
- 增加 Intents 合并回归测试。

## 1.0.1 - 2026-07-14

- 修复从 AstrBot 管理面板安装、更新或热重载后，已有 QQ 官方平台实例没有接入通知回调的问题。
- 修复退群通知错误携带 `GROUP_MEMBER_REMOVE` 事件 ID，可能被 QQ 群消息接口拒绝的问题。
- 补充热重载绑定和退群主动消息的回归测试。

## 1.0.0 - 2026-07-14

- 云云完成首个版本。
- 支持机器人加入群聊时发送自定义打招呼消息。
- 支持普通成员进群欢迎和退群通知。
- 为 AstrBot 原生 QQ WebSocket/Webhook 适配器补充可卸载的成员事件解析桥。
- 三类通知分别提供开关和模板。
- 支持群黑名单、白名单和群名称映射。
- 支持事件字段、运行期昵称缓存和 OpenID 回退。
- 加入 `event_id` 去重和调试日志。
- 加入管理员 `/群通知信息` 指令，便于查看当前群 OpenID、群名称和过滤结果。
